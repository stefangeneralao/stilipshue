import { ContentWrapper } from '../../common/ContentWrapper';
import styled from 'styled-components';

export const Jobs = styled(ContentWrapper)``;

export const Job = styled.div`
  margin-bottom: 100px;
`;
